name: Deploy via ftp
on: push
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Upload ftp
      uses: sebastianpopp/ftp-action@releases/v2
      with:
        host: ftp.1664532155313-sh.bigrock.in
        user: bukant@bukant.in
        password: bukant_pass2022
        localDir: "/"
        remoteDir: "public_html/www/bukant.in/"
        options: "--delete --asci"
